// defining magic string to avoid bugs
//created magic string to avoid bug without magic string  , difiiculty to find bug using tools as string not rely on variable
module.exports = {
    events:{
        GREET: 'greet'
    }
}